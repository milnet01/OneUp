"""The window, split into modules that each do one job.

Deliberately empty of logic and of re-exports: a name bound here would be a
second home for something that already has one, and a test reassigning the
re-export would patch a binding nobody reads
(docs/specs/ONEUP-0034-gui-modules.md §4.4).
"""
